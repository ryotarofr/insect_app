/target

# ts-rs が `cargo test` 時に生成する TypeScript 型定義の中間出力。
# これを集約した最終物 `client_solid/src/generated/sdui.ts` のみ Git 管理する
# (集約スクリプト: client_solid/scripts/gen-sdui-types.mjs)
/bindings

# 環境変数 (= 開発者 local の DB 接続情報 / Stripe sk_test_ 等)。
# テンプレートのみ .env.example として管理、実体は git 管理しない。
.env
.env.local
.env.*.local
